# Copy to .env and adjust. Secrets live here, never in code (SPEC §3).
DATABASE_URL=postgresql+psycopg://zaspro:zaspro@localhost:5432/zaspro
# Only needed once AI agents run (M3+). Leave blank for M1/M2.
ANTHROPIC_API_KEY=
STORAGE_ROOT=./storage
