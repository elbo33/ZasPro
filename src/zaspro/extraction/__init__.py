"""Track A extraction: deterministic DOCX -> LaTeX -> segmented exercise chunks.

No model calls anywhere in this package. Every artifact it produces carries
``confidence = None`` (deterministic) per SPEC section 2a / 5.
"""
